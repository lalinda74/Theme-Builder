import styled from "styled-components";

export const StyledFooter = styled.footer(({theme}) =>`
    margin-top: 5rem;
    text-align: center;

    > p {
        font-size: 0.9rem;
        opacity: 0.7;
    }
`);